Metadata-Version: 2.4
Name: contract-parser
Version: 0.1.0
Summary: PDF contract parser extracting pricing rules, surcharges, discount tiers, and service terms into a canonical model.
Requires-Python: >=3.9
Requires-Dist: pdfplumber>=0.11.4
Requires-Dist: pydantic>=2.8.2
Requires-Dist: python-dateutil>=2.9.0.post0
Requires-Dist: rich>=13.8.1
Requires-Dist: typer>=0.12.5
Description-Content-Type: text/markdown

# 71lbs-project
Contract extraction and invoice auditing for 71lbs by VCG.

## Contract PDF parser (canonical JSON)

This repo includes a baseline PDF contract parser that extracts:
- structured **pricing rules**
- **surcharge tables**
- **discount tiers**
- **service-level terms**
- plus the underlying segmented **sections**, **tables**, and **footnotes**

### Install

```bash
python3 -m pip install -e .
```

### Parse a PDF

```bash
contract-parse parse "/path/to/contract.pdf" --out contract.json
```

### Output shape

The parser emits a machine-readable JSON document (`schema_version: "1.0"`) with:
- `metadata.vendor_type`: `fedex | ups | 3pl | freight | unknown`
- `sections[]`: each section includes `raw_text`, `tables[]`, `footnotes[]` and extracted:
  - `extracted_pricing_rules[]`
  - `extracted_surcharge_tables[]`
  - `extracted_discount_tiers[]`
  - `extracted_service_terms[]`

### Refined pipeline (v2 — validation and confidence)

The `contract_pipeline_refined` package iterates on the same extraction and sectionization flow, then applies **normalization** (service names, percents, weight ranges), **validation issues**, and **confidence scoring** for auditing.

```bash
contract-parse-refined parse "/path/to/contract.pdf" --out contract_v2.json
```

Output uses `schema_version: "2.0"` with a nested `document` (same shape as v1) plus `issues`, `validation_summary`, and `confidence`.
